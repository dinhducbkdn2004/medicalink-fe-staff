# src/common

The common directory (also called lib) contains files common to the entire application.

Currently, the directory contains a `types.ts` file with exported types for function components and common application types.
